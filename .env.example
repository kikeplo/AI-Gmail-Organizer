# OpenAI (optional)
# Set an API key and model locally if you want AI-assisted classification and chat.
OPENAI_API_KEY=
OPENAI_MODEL=

# Gmail OAuth client settings are optional environment overrides.
# The default credentials file is credentials.json in the project root.
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
